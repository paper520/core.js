import castPath from './castPath';

/**
 *
 * @private
 * @param {*} object
 * @param {*} path
 * @param {*} value
 * @param {*} customizer
 */
export default function(object, path, value, customizer) {

};
